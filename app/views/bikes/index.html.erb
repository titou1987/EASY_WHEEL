
<%# <% bikes.each do |bike| %>
  <a href="<%= bike_url(bike) %>" class="card-product-link">
    <div class="card-product">
      <img src="https://www.amsterdamair.fr/img/c/5275.jpg" alt="<%= bike.name %>"/>
      <div class="card-product-infos">
        <h2><%= bike.name %></h2>
        <h3><%= bike.price %></h3>
        <p><%= bike.desription %></p>
      </div>
    </div>
  </a>
<%# <% end %>
